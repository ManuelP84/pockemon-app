import * as React from 'react';

interface IPockemonCardProps {
}

const PockemonCard: React.FunctionComponent<IPockemonCardProps> = (props) => {
  return(
    <div>
        Pockemon card Here!
    </div>
   ) ;
};

export default PockemonCard;
