import * as React from 'react';

interface IPockemonFilterProps {
}

const PockemonFilter: React.FunctionComponent<IPockemonFilterProps> = (props) => {
  return (
    <div>
        Pockemon Filter Here!
    </div>
  ) ;
};

export default PockemonFilter;
