import * as React from 'react';

interface IFavoritesProps {
}

const Favorites: React.FunctionComponent<IFavoritesProps> = (props) => {
  return (
    <div>
        Here goes the favorites
    </div>
  );
};

export default Favorites;
